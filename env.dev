# IDs
##################

USER_ID=1000
GROUP_ID=1000

# Container config
##################

RDM_VERSION=1.7
BASE_VERSION=6.5-noble
DATAVERSE_WAR_URL=https://github.com/IQSS/dataverse/releases/download/v6.4/dataverse-6.4.war

# Image tags
############
REPOSITORY=
DATAVERSE_BASE_IMAGE_TAG=${REPOSITORY}rdm/dataverse-base:${RDM_VERSION}
DATAVERSE_CONFIG_IMAGE_TAG=${REPOSITORY}rdm/dataverse-config:${RDM_VERSION}
DATAVERSE_STOCK_IMAGE_TAG=${REPOSITORY}rdm/dataverse-stock:${RDM_VERSION}
DATAVERSE_IMAGE_TAG=${REPOSITORY}rdm/dataverse:${RDM_VERSION}
PROXY_IMAGE_TAG=${REPOSITORY}rdm/proxy:${RDM_VERSION}
DATAVERSE_GLOBUS_IMAGE_TAG=${REPOSITORY}rdm/dataverse-globus:${RDM_VERSION}
