# nginx.conf.template
server {
    listen              ${NGINX_HTTP_PORT};
    server_name         localhost;    

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }
}